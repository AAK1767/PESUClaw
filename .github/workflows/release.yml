name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Version tag (e.g. v1.1.0)'
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build extensions
        run: |
          chmod +x build.sh
          ./build.sh

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "tag=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          fi

      - name: Package Chrome extension
        run: |
          cd build/chrome
          zip -r "../../PESUClaw-Chrome-${{ steps.version.outputs.tag }}.zip" .

      - name: Package Firefox extension
        run: |
          cd build/firefox
          zip -r "../../PESUClaw-Firefox-${{ steps.version.outputs.tag }}.zip" .

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: PESUClaw ${{ steps.version.outputs.tag }}
          body: |
            ## Installation

            ### Chrome (recommended)
            1. Download **PESUClaw-Chrome-${{ steps.version.outputs.tag }}.zip**
            2. Unzip it to a folder
            3. Open `chrome://extensions` → enable **Developer mode**
            4. Click **Load unpacked** → select the unzipped folder
            5. The extension stays installed until you remove it

            ### Firefox
            1. Download **PESUClaw-Firefox-${{ steps.version.outputs.tag }}.zip**
            2. Unzip it to a folder
            3. Open `about:debugging` → **This Firefox** → **Load Temporary Add-on**
            4. Select `manifest.json` inside the unzipped folder

            > ⚠️ Firefox temporary add-ons are removed when you close Firefox. You'll need to repeat steps 3–4 each time you restart the browser.
          files: |
            PESUClaw-Chrome-${{ steps.version.outputs.tag }}.zip
            PESUClaw-Firefox-${{ steps.version.outputs.tag }}.zip
          generate_release_notes: true
